# Name for the microservice
spring.application.name=limits-service

# Since this is now bootstrap properties, we can configure the service to use the spring cloud
# config server by configuring the uri
spring.cloud.config.uri=http://localhost:8888

# Enable dev profile
spring.profiles.active=dev